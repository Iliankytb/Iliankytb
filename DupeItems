local player = Game.Players.LocalPlayer --le joueur

local function dupeItems(tool) --la fonction
    for _, tool in pairs(player.Character:GetChildren()) do
        local clone = tool:Clone()
        tool.Parent = player.Backpack
    end
end

dupeItems()
